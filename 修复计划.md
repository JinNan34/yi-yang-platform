# 医生服务平台 · 前端与体验优化及常见错误修复计划

本文档基于当前仓库结构（`doctor-platform-frontend`：Vue 3 + Vite + Element Plus + Pinia；`doctor-platform-backend`：Spring Boot 3 + Security + JWT）做一次面向「排版布局、交互体验、稳定性与常见坑」的修复与优化计划，便于按阶段实施与验收。

---

## 一、现状简要结论

| 维度 | 现状 | 风险或改进空间 |
|------|------|----------------|
| 整体布局 | `MainLayout` 固定侧栏 220px + 顶栏 + `el-main` 灰底 | 窄屏/平板上侧栏占满宽度、表格横向溢出、无折叠菜单 |
| 视觉一致性 | 主题色 `#1a5f7a` 与登录页渐变统一 | 各业务页多为裸 `el-card`，标题层级、间距、最大内容宽未统一 |
| 请求层 | `axios` 封装、`code !== 0` 统一 `ElMessage` | 业务层 `catch` 可能重复弹窗；超时/断网文案可细化 |
| 安全与演示 | `Login.vue` 预填演示账号与明文提示 | 正式环境易误用；需环境区分或构建开关 |
| 后端与前端契约 | `ApiResult` + 全局异常处理较完整 | 个别路径与配置组合易产生「隐蔽」错误（见下文第三节） |

---

## 二、前端排版与布局优化计划

### 2.1 全局壳层（`MainLayout.vue`）

1. **响应式侧栏**：小屏（如 `< 992px`）改为抽屉式侧栏或图标折叠菜单，避免遮挡主内容；顶栏增加「菜单」按钮触发。
2. **主内容区规范**：为 `el-main` 增加统一内边距（如 `16px 20px`）与可选 `max-width` + 居中，避免超宽显示器上表格拉得过长、阅读困难。
3. **顶栏信息架构**：左侧可放当前模块标题或面包屑（由 `route.meta.title` 驱动），右侧保留欢迎语与退出，符合后台系统常见习惯。
4. **侧栏长菜单**：菜单项较多时考虑分组（「业务数据」「系统与账户」）或 `el-sub-menu`，降低认知负担。

### 2.2 列表页模板（如 `Elders.vue` 及各模块列表）

1. **工具栏**：统一为「搜索区 + 操作按钮」换行或 `flex-wrap`，小屏时按钮不挤压输入框。
2. **表格**：为常用列表统一 `table-layout`、合理 `min-width`、关键列 `fixed`，并保持 `empty-text` 与 `v-loading` 一致文案风格。
3. **分页**：统一 `layout`（建议含 `sizes` 与 `jumper` 按需开启），分页变更时滚动到表格顶部或保持视口稳定（择一作为规范）。

### 2.3 表单与弹窗

1. **弹窗宽度**：窄屏下将固定 `width="560px"` 改为 `width="90%"` + `max-width`，避免溢出。
2. **表单项**：长表单考虑分步或 `el-tabs` 分组（基本信息 / 联系与紧急联系人等），减少一屏压力。

### 2.4 设计令牌（可选，工作量中等）

抽取 CSS 变量或少量 SCSS：`--app-primary`、`--app-bg-page`、`--app-radius-card`，在 `App.vue` 或全局样式中挂载，减少魔法色散落于各页。

---

## 三、用户交互体验优化计划

### 3.1 反馈与状态

1. **登录**：失败时在页面内展示简要错误（除 `ElMessage` 外），并对「限流 429」给出可操作提示（稍后再试）；`onSubmit` 中建议显式 `catch`，避免仅依赖拦截器导致行为不统一。
2. **异步操作**：保存、删除、上传等已有 `loading` 的保持；缺失的补全，并避免连点重复提交（按钮 `loading` + 防抖双保险）。
3. **危险操作**：删除类操作统一 `ElMessageBox.confirm`，文案含业务主键信息（如老人姓名）以防误删。

### 3.2 导航与权限（后续若后端细化角色）

1. 路由 `meta` 增加 `title`、`roles`，侧栏按角色过滤菜单项，避免无权限用户点进页面再 403。
2. 收到 403 时给出明确说明并引导返回首页或联系管理员。

### 3.3 无障碍与细节

1. 表单 `label` 与 `aria`、登录页键盘提交已有基础，可检查焦点顺序与弹窗打开后焦点落入第一个输入框（`Elders` 等已有 `nameInputRef` 模式可复用）。

### 3.4 个人账户（`Profile.vue`）

1. 头像预览：上传成功后立即更新预览；若后端返回相对路径，统一通过「站点根或 `/api` 前缀」拼接，避免开发与生产环境表现不一致。
2. `avatarUrl` 计算属性：对「非 `http`」路径应规范拼接基地址（与 `vite` 代理、`Nginx` 部署前缀一致），并单独处理异常形态 URL（见第四节）。

---

## 四、常见错误与缺陷（优先修复项）

### 4.1 头像存储 URL 与浏览器解析（高优先级）

**现象**：`ProfileController` 中头像地址使用 `contextPath + "/files/" + filename`，而配置中 `server.servlet.context-path` 为 `/` 时，`contextPath` 为 `/`，拼接结果可能为 `//files/xxx.png`。

**风险**：在 HTML/JS 中，`src` 以 `//` 开头会被视为「协议相对 URL」，可能指向错误主机或无法加载头像。

**建议**：统一为明确路径，例如始终以 `/api/files/` 为前缀（与 `FileController` 的 `@RequestMapping("/api/files")` 一致），或使用 `ServletUriComponentsBuilder` / 配置的 `app.public-base-url` 生成绝对 URL；并加一条集成测试或手动用例清单。

### 4.2 开发代理与生产部署

**现象**：前端 `baseURL: '/api'` 依赖 Vite `proxy` 转发到 `8083`。

**风险**：生产环境若未配置同源反向代理，静态资源与接口路径易 404 或跨域。

**建议**：文档化部署拓扑（Nginx 将 `/api` 反代到后端）；可选在构建时使用 `import.meta.env.VITE_API_BASE` 区分环境。

### 4.3 错误提示重复或含糊

**现象**：拦截器已对 `code !== 0` 弹窗，若业务 `catch` 再 `ElMessage`，用户会看到两次提示。

**建议**：约定「仅拦截器负责全局 Toast」或「仅业务层处理可预期错误」，并在代码审查中检查。

### 4.4 全局 401 与登录页

**现象**：拦截器在 401 时 `clear` + `push('/login')`，部分接口可能同时触发多处请求导致多次跳转或多次提示。

**建议**：401 处理防抖或「仅首次跳转」标志；登录页对 `redirect` 查询参数做合法校验（防开放重定向）。

### 4.5 后端 500 暴露内部信息

**现象**：`GlobalExceptionHandler` 对通用 `Exception` 返回 `e.getMessage()`。

**风险**：生产环境可能泄露堆栈或 SQL 片段。

**建议**：生产 profile 下返回固定文案「服务器繁忙」，详细错误仅打日志。

### 4.6 演示账号与默认密码

**现象**：登录页写死演示账号提示、默认填 `doctor` / `123456`。

**建议**：仅 `import.meta.env.DEV` 或独立「演示构建」显示；正式构建移除预填与文案。

---

## 五、分阶段实施建议

### 阶段 A（1～2 天）：缺陷与一致性「止血」

- 修复头像 URL 拼接问题，并验证上传后在开发/模拟生产路径下均能显示。
- 梳理 `http.js` 与各页 `catch` 的重复弹窗。
- 登录失败与 429 的提示与 `loading` 状态收尾。

### 阶段 B（3～5 天）：布局与响应式

- `MainLayout` 响应式侧栏 + 主区内边距与可选最大宽度。
- 抽 1～2 个典型列表页做模板，其余页面对齐工具栏与表格规范。

### 阶段 C（按需）：权限、部署与工程化

- 路由级角色与菜单过滤；环境变量与部署说明。
- 后端生产环境错误脱敏；可选接入前端错误边界或统一埋点。

---

## 六、验收建议（每阶段可勾选）

- [ ] 浏览器宽度 375px / 768px / 1280px 下主导航与列表无严重重叠或横向裁切。
- [ ] 头像上传 → 刷新页面 → 预览仍正确（相对/绝对路径两种数据各测一次）。
- [ ] 断网、超时、401、403、429、业务 `code !== 0` 各至少手测一次，无重复弹窗、无白屏。
- [ ] 生产构建下无演示账号文案与无默认密码填充（若已启用环境区分）。

---

## 七、文档维护

本计划在实施过程中随代码变更更新：每完成一节，在对应小节末尾标注完成日期与 PR/提交说明，便于实训报告或答辩材料引用。
