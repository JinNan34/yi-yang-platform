# 智慧医养医生服务系统 - 项目分析报告

## 一、项目概述

### 1.1 项目简介

**智慧医养医生服务系统**是一个面向医护人员的医疗服务管理平台，主要用于管理老年人的健康档案、健康预警、随访干预等业务。系统采用前后端分离架构，后端提供 RESTful API，前端提供友好的操作界面。

### 1.2 技术栈

| 分类 | 技术 | 版本 |
| :--- | :--- | :--- |
| 后端框架 | Spring Boot | 3.2.5 |
| 数据库 | MySQL | 8.0+ |
| ORM框架 | MyBatis Plus | 3.5.5 |
| 安全框架 | Spring Security | 6.2.x |
| 认证方式 | JWT | 0.12.5 |
| 缓存 | Redis | 7.0+ |
| 前端框架 | Vue | 3.4.21 |
| UI组件 | Element Plus | 2.6.3 |
| 状态管理 | Pinia | 2.1.7 |
| 路由 | Vue Router | 4.3.0 |
| 构建工具 | Vite | 5.2.8 |

---

## 二、项目架构

### 2.1 架构设计

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端层 (Vue 3)                           │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│  │ 登录页  │ │ 老人管理│ │健康记录 │ │健康预警 │ │随访管理 │   │
│  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘   │
└───────┼───────────┼───────────┼───────────┼───────────┼───────┘
        │           │           │           │           │
        ▼           ▼           ▼           ▼           ▼
┌─────────────────────────────────────────────────────────────────┐
│                      后端层 (Spring Boot)                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │ AuthController│ │ElderController││HealthController│          │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘            │
│         │                │                │                     │
│  ┌──────▼──────┐  ┌──────▼──────┐  ┌──────▼──────┐            │
│  │ Security    │  │ Service     │  │ Mapper      │            │
│  │ (JWT/权限)  │  │ (业务逻辑)   │  │ (数据访问)  │            │
│  └─────────────┘  └─────────────┘  └─────────────┘            │
└───────────────────────────┬─────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   MySQL     │    │    Redis    │    │   文件存储  │
│ (业务数据)   │    │ (缓存/限流) │    │  (头像等)   │
└─────────────┘    └─────────────┘    └─────────────┘
```

### 2.2 模块划分

| 模块 | 功能描述 | 主要文件 |
| :--- | :--- | :--- |
| **认证模块** | 医生登录、JWT令牌管理 | `AuthController`, `JwtUtil`, `SecurityConfig` |
| **老人管理** | 老人档案的增删改查 | `ElderController`, `ElderMapper`, `Elder` |
| **健康记录** | 血压、血糖、心率等记录 | `HealthRecordController`, `HealthRecordMapper` |
| **健康预警** | 异常指标预警管理 | `HealthAlertController`, `HealthAlertMapper` |
| **重点人群随访** | 高危人群随访计划 | `KeyPopulationFollowupController` |
| **随访干预** | 干预记录管理 | `FollowupInterventionController` |
| **健康评估** | 老人健康评分评估 | `HealthAssessmentController` |
| **账户管理** | 老人账户余额管理 | `ElderAccountController` |
| **权限服务** | 数据权限校验 | `RecordPermissionService` |

---

## 三、目录结构

### 3.1 后端目录结构

```
doctor-platform-backend/
├── src/main/java/com/medical/doctorplatform/
│   ├── common/           # 公共组件
│   │   ├── ApiResult.java           # 统一响应封装
│   │   ├── GlobalExceptionHandler.java  # 全局异常处理
│   │   └── ForbiddenOperationException.java  # 权限异常
│   ├── config/           # 配置类
│   │   ├── DataInitializer.java     # 数据初始化
│   │   ├── MybatisPlusConfig.java   # MyBatis配置
│   │   ├── SchemaPatcher.java       # 数据库补丁
│   │   └── WebMvcConfig.java        # Web配置
│   ├── controller/       # REST API控制器
│   │   ├── AuthController.java      # 认证接口
│   │   ├── ElderController.java     # 老人管理
│   │   ├── HealthRecordController.java  # 健康记录
│   │   ├── HealthAlertController.java   # 健康预警
│   │   ├── KeyPopulationFollowupController.java  # 重点人群随访
│   │   ├── FollowupInterventionController.java   # 随访干预
│   │   ├── HealthAssessmentController.java       # 健康评估
│   │   ├── ElderAccountController.java           # 老人账户
│   │   └── ProfileController.java    # 医生档案
│   ├── domain/           # 领域模型
│   │   └── DoctorRole.java          # 医生角色枚举
│   ├── dto/              # 数据传输对象
│   │   ├── LoginRequest.java        # 登录请求
│   │   ├── LoginResponse.java       # 登录响应
│   │   └── PasswordUpdateRequest.java   # 修改密码
│   ├── entity/           # 数据库实体
│   │   ├── Doctor.java              # 医生实体
│   │   ├── Elder.java               # 老人实体
│   │   ├── ElderAccount.java        # 老人账户
│   │   ├── HealthRecord.java        # 健康记录
│   │   ├── HealthAlert.java         # 健康预警
│   │   ├── KeyPopulationFollowup.java   # 重点人群随访
│   │   ├── FollowupInterventionRecord.java  # 随访干预记录
│   │   └── HealthAssessment.java        # 健康评估
│   ├── mapper/           # MyBatis Mapper接口
│   ├── security/         # 安全组件
│   │   ├── SecurityConfig.java       # 安全配置
│   │   ├── JwtUtil.java              # JWT工具
│   │   ├── JwtAuthenticationFilter.java  # JWT过滤器
│   │   ├── DoctorUserDetailsService.java # 用户详情服务
│   │   └── LoginUser.java            # 登录用户对象
│   ├── service/          # 业务服务
│   │   ├── LoginRateLimiter.java     # 登录限流
│   │   └── RecordPermissionService.java  # 权限校验
│   ├── util/             # 工具类
│   │   └── SecurityUtils.java        # 安全工具
│   └── DoctorPlatformApplication.java  # 启动类
├── src/main/resources/
│   └── application.yml   # 应用配置
└── pom.xml               # Maven依赖管理
```

### 3.2 前端目录结构

```
doctor-platform-frontend/
├── src/
│   ├── api/              # API封装
│   │   └── http.js       # Axios配置与拦截器
│   ├── router/           # 路由配置
│   │   └── index.js      # 路由定义与守卫
│   ├── stores/           # 状态管理
│   │   └── user.js       # 用户状态
│   ├── utils/            # 工具函数
│   │   └── validators.js # 表单验证
│   ├── views/            # 页面组件
│   │   ├── Login.vue         # 登录页
│   │   ├── MainLayout.vue    # 主布局
│   │   ├── Home.vue          # 首页
│   │   ├── Elders.vue        # 老人管理
│   │   ├── HealthRecords.vue # 健康记录
│   │   ├── HealthAlerts.vue  # 健康预警
│   │   ├── KeyFollowups.vue  # 重点人群随访
│   │   ├── FollowupInterventions.vue  # 随访干预
│   │   ├── HealthAssessments.vue      # 健康评估
│   │   ├── ElderAccounts.vue          # 老人账户
│   │   └── Profile.vue       # 医生档案
│   ├── App.vue           # 根组件
│   └── main.js           # 入口文件
├── index.html            # HTML模板
├── package.json          # 依赖配置
└── vite.config.js        # Vite配置
```

---

## 四、核心功能分析

### 4.1 认证模块

#### 4.1.1 功能特性

| 特性 | 描述 |
| :--- | :--- |
| **JWT认证** | 使用JJWT库生成和验证JWT令牌，有效期24小时 |
| **登录限流** | Redis实现登录失败次数限制，5次失败后锁定15分钟 |
| **密码加密** | 使用BCryptPasswordEncoder加密存储 |
| **自动刷新** | 前端拦截器检测401状态自动跳转登录 |

#### 4.1.2 关键代码

**JWT生成逻辑** (`JwtUtil.java:27-35`):
```java
public String createToken(Doctor doctor) {
    Date now = new Date();
    return Jwts.builder()
            .subject(doctor.getUsername())
            .claim("uid", doctor.getId())
            .issuedAt(now)
            .expiration(new Date(now.getTime() + expirationMs))
            .signWith(key())
            .compact();
}
```

### 4.2 老人管理模块

#### 4.2.1 功能特性

| 特性 | 描述 |
| :--- | :--- |
| **档案创建** | 自动关联建档医生ID，自动创建老人账户 |
| **分页查询** | 支持按姓名模糊搜索，按创建时间倒序 |
| **数据权限** | 仅建档医生、科室负责人、管理员可修改 |
| **级联删除** | 删除老人时需校验权限 |

#### 4.2.2 数据权限规则

```
┌──────────────────────────────────────────────────────────────┐
│                    数据权限判断流程                           │
├──────────────────────────────────────────────────────────────┤
│  当前用户 ──→ 是管理员？ ──→ 是 ──→ 允许操作                │
│      │                                                       │
│      否                                                       │
│      ↓                                                       │
│  记录有创建者？ ──→ 否 ──→ 是科室负责人？ ──→ 是 ──→ 允许    │
│      │                                                       │
│      是                                                       │
│      ↓                                                       │
│  当前用户是创建者？ ──→ 是 ──→ 允许操作                       │
│      │                                                       │
│      否                                                       │
│      ↓                                                       │
│  当前用户是科室负责人且创建者同科室？ ──→ 是 ──→ 允许操作     │
│      │                                                       │
│      否                                                       │
│      ↓                                                       │
│  拒绝操作（抛出ForbiddenOperationException）                 │
└──────────────────────────────────────────────────────────────┘
```

### 4.3 健康记录模块

#### 4.3.1 记录类型

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| systolic_bp | INT | 收缩压 (mmHg) |
| diastolic_bp | INT | 舒张压 (mmHg) |
| blood_sugar | DECIMAL(6,2) | 血糖值 (mmol/L) |
| heart_rate | INT | 心率 (次/分钟) |
| temperature | DECIMAL(4,1) | 体温 (℃) |
| weight | DECIMAL(5,2) | 体重 (kg) |

### 4.4 健康预警模块

#### 4.4.1 预警级别

| 级别 | 说明 |
| :--- | :--- |
| HIGH | 高危预警（如血压危急值） |
| MEDIUM | 中危预警（如血糖偏高） |
| LOW | 低危预警（如体重波动） |

#### 4.4.2 预警状态

| 状态码 | 说明 |
| :--- | :--- |
| 0 | 待处理 |
| 1 | 已处理 |

---

## 五、数据库设计

### 5.1 数据库表总览

| 表名 | 说明 | 关键字段 |
| :--- | :--- | :--- |
| doctor | 医生表 | id, username, password, role, department |
| elder | 老人表 | id, name, id_card, creator_doctor_id |
| health_record | 健康记录表 | id, elder_id, doctor_id, systolic_bp, blood_sugar |
| health_alert | 健康预警表 | id, elder_id, alert_type, alert_level, status |
| key_population_followup | 重点人群随访表 | id, elder_id, risk_type, followup_cycle_days |
| followup_intervention_record | 随访干预记录表 | id, followup_id, elder_id, intervention_type |
| health_assessment | 健康评估表 | id, elder_id, assessor_id, score, conclusion |
| elder_account | 老人账户表 | id, elder_id, account_no, balance |

### 5.2 核心表关系图

```
doctor 1 ─── * elder 1 ─── * health_record
  │              │              │
  │              │              ▼
  │              │       health_alert
  │              │              │
  │              ▼              ▼
  │       elder_account   key_population_followup 1 ─── * followup_intervention_record
  │                               │
  ▼                               ▼
health_assessment (assessor_id)
```

### 5.3 表结构详情

#### doctor 表

| 字段 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| id | BIGINT | PRIMARY KEY AUTO_INCREMENT | 主键 |
| username | VARCHAR(64) | NOT NULL UNIQUE | 用户名 |
| password | VARCHAR(128) | NOT NULL | 密码(BCrypt) |
| real_name | VARCHAR(64) | | 真实姓名 |
| title | VARCHAR(64) | | 职称 |
| department | VARCHAR(128) | | 科室 |
| phone | VARCHAR(32) | | 电话 |
| avatar | VARCHAR(512) | | 头像URL |
| role | VARCHAR(32) | | 角色: DOCTOR/DEPT_HEAD/ADMIN |
| status | TINYINT | DEFAULT 1 | 状态(1启用/0禁用) |
| deleted | TINYINT | DEFAULT 0 | 逻辑删除 |
| create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

#### elder 表

| 字段 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| id | BIGINT | PRIMARY KEY AUTO_INCREMENT | 主键 |
| creator_doctor_id | BIGINT | | 建档医生ID |
| name | VARCHAR(64) | NOT NULL | 姓名 |
| id_card | VARCHAR(32) | | 身份证号 |
| gender | TINYINT | | 性别 |
| birth_date | DATE | | 出生日期 |
| phone | VARCHAR(32) | | 电话 |
| address | VARCHAR(512) | | 住址 |
| emergency_contact | VARCHAR(64) | | 紧急联系人 |
| emergency_phone | VARCHAR(32) | | 紧急联系电话 |
| remark | VARCHAR(512) | | 备注 |
| deleted | TINYINT | DEFAULT 0 | 逻辑删除 |
| create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

---

## 六、安全机制

### 6.1 JWT令牌机制

#### 6.1.1 令牌结构

```
Header.Payload.Signature

Header: {"alg":"HS256","typ":"JWT"}
Payload: {
  "sub": "doctor001",      // 用户名
  "uid": 1,                // 用户ID
  "iat": 1715400000,       // 签发时间
  "exp": 1715486400        // 过期时间
}
```

#### 6.1.2 验证流程

```
请求 → JwtAuthenticationFilter → 解析Token → 获取Username → 
DoctorUserDetailsService → 加载UserDetails → 
存入SecurityContext → Controller处理
```

### 6.2 登录限流机制

#### 6.2.1 限流策略

| 参数 | 值 | 说明 |
| :--- | :--- | :--- |
| max-failures | 5 | 最大失败次数 |
| lock-ttl-minutes | 15 | 锁定时长(分钟) |

#### 6.2.2 Redis Key设计

```
doctor:login:failures:{username}  → 失败次数
doctor:login:blocked:{username}   → 锁定标记(带TTL)
```

### 6.3 CORS配置

后端已通过 `WebMvcConfig` 配置跨域支持，允许前端访问。

---

## 七、API接口汇总

### 7.1 认证接口

| HTTP方法 | 路径 | 功能 |
| :--- | :--- | :--- |
| POST | /api/auth/login | 医生登录 |

### 7.2 老人管理接口

| HTTP方法 | 路径 | 功能 |
| :--- | :--- | :--- |
| GET | /api/elders | 分页查询老人列表 |
| GET | /api/elders/{id} | 获取老人详情 |
| POST | /api/elders | 创建老人档案 |
| PUT | /api/elders/{id} | 更新老人信息 |
| DELETE | /api/elders/{id} | 删除老人档案 |

### 7.3 健康记录接口

| HTTP方法 | 路径 | 功能 |
| :--- | :--- | :--- |
| GET | /api/health-records | 分页查询健康记录 |
| GET | /api/health-records/{id} | 获取健康记录详情 |
| POST | /api/health-records | 创建健康记录 |
| PUT | /api/health-records/{id} | 更新健康记录 |
| DELETE | /api/health-records/{id} | 删除健康记录 |
| GET | /api/health-records/elder/{elderId} | 查询指定老人的健康记录 |

### 7.4 健康预警接口

| HTTP方法 | 路径 | 功能 |
| :--- | :--- | :--- |
| GET | /api/health-alerts | 分页查询预警列表 |
| PUT | /api/health-alerts/{id}/handle | 处理预警 |

### 7.5 随访管理接口

| HTTP方法 | 路径 | 功能 |
| :--- | :--- | :--- |
| GET | /api/key-followups | 分页查询重点人群随访 |
| POST | /api/key-followups | 创建随访计划 |
| PUT | /api/key-followups/{id} | 更新随访计划 |
| DELETE | /api/key-followups/{id} | 删除随访计划 |

| HTTP方法 | 路径 | 功能 |
| :--- | :--- | :--- |
| GET | /api/followup-interventions | 分页查询干预记录 |
| POST | /api/followup-interventions | 创建干预记录 |

### 7.6 健康评估接口

| HTTP方法 | 路径 | 功能 |
| :--- | :--- | :--- |
| GET | /api/health-assessments | 分页查询评估记录 |
| POST | /api/health-assessments | 创建评估记录 |

### 7.7 账户管理接口

| HTTP方法 | 路径 | 功能 |
| :--- | :--- | :--- |
| GET | /api/elder-accounts | 分页查询老人账户 |
| GET | /api/elder-accounts/{id} | 获取账户详情 |
| PUT | /api/elder-accounts/{id} | 更新账户信息 |

### 7.8 医生档案接口

| HTTP方法 | 路径 | 功能 |
| :--- | :--- | :--- |
| GET | /api/profile | 获取当前医生信息 |
| PUT | /api/profile | 更新医生信息 |
| PUT | /api/profile/password | 修改密码 |

---

## 八、部署与运行

### 8.1 环境要求

| 依赖 | 版本 |
| :--- | :--- |
| JDK | 17+ |
| Maven | 3.8+ |
| MySQL | 8.0+ |
| Redis | 7.0+ |
| Node.js | 18+ |

### 8.2 启动步骤

#### 8.2.1 后端启动

```bash
cd doctor-platform-backend
# 编译项目
mvn clean package -DskipTests
# 运行
java -jar target/doctor-platform-backend-1.0.0.jar
```

#### 8.2.2 前端启动

```bash
cd doctor-platform-frontend
# 安装依赖
npm install
# 开发模式
npm run dev
# 生产构建
npm run build
```

### 8.3 配置说明

后端配置文件 `application.yml`:

```yaml
server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/doctor_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  redis:
    host: localhost
    port: 6379

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

app:
  jwt:
    secret: DoctorPlatformJwtSecretKeyMustBe256BitsLongForHS256Algorithm!!
    expiration-ms: 86400000
  login:
    max-failures: 5
    lock-ttl-minutes: 15
```

---

## 九、代码质量评估

### 9.1 优点

| 维度 | 评估 | 说明 |
| :--- | :--- | :--- |
| **架构设计** | 良好 | 前后端分离，职责清晰 |
| **安全机制** | 良好 | JWT认证、登录限流、数据权限控制完整 |
| **代码规范** | 良好 | 使用Lombok简化代码，命名规范 |
| **异常处理** | 良好 | 全局异常处理器统一响应格式 |
| **数据库设计** | 良好 | 表结构合理，索引设计完善 |

### 9.2 改进建议

| 序号 | 问题 | 建议 |
| :--- | :--- | :--- |
| 1 | Service层缺失 | 业务逻辑直接在Controller中处理，建议抽取Service层 |
| 2 | 日志缺失 | 关键业务操作缺乏日志记录 |
| 3 | 接口文档缺失 | 未提供Swagger/OpenAPI文档 |
| 4 | 测试用例缺失 | 缺少单元测试和集成测试 |
| 5 | 密码硬编码 | 配置文件中密码应通过环境变量注入 |
| 6 | JWT密钥硬编码 | 生产环境应使用安全的密钥管理 |

---

## 十、总结

### 10.1 项目价值

本项目是一个功能完整的智慧医养医生服务系统，具备以下核心价值：

1. **规范化管理**: 实现老年人健康档案的电子化、规范化管理
2. **风险预警**: 通过健康数据监测实现异常指标自动预警
3. **随访闭环**: 重点人群随访计划与干预记录形成完整闭环
4. **权限管控**: 多层次数据权限确保信息安全

### 10.2 未来扩展方向

1. **移动端适配**: 开发微信小程序或App端
2. **数据分析**: 增加健康趋势分析、报表统计功能
3. **消息推送**: 预警消息实时推送至医生端
4. **多机构支持**: 支持多养老院/医疗机构数据隔离
5. **AI辅助诊断**: 集成AI模型辅助健康风险评估

---

**文档生成时间**: 2026-05-11  
**项目版本**: 1.0.0